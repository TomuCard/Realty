// Profile
document;
document
  .querySelector(".global-saveButton")
  .addEventListener("click", function (event) {
    if (event.type === "click") {
      var profileCard = document.querySelector(".profile-card");
      profileCard.classList.toggle("flipped");
    }
  });

document;
document
  .querySelector("#saveProfile")
  .addEventListener("click", function (event) {
    if (event.type === "click") {
      var profileCard = document.querySelector(".profile-card");
      profileCard.classList.toggle("flipped");
    }
  });
